import type { GalleryImage } from '@/types/gallery'
import { weddingImages } from '@/data/gallery/wedding'

/** The single curated image shown in the home page hero */
export const heroImage: GalleryImage = weddingImages[0]
